/**
 * The Play Store app (roadmap A-02) is a Trusted Web Activity with this package name. Keep it in
 * sync with `apps/android/twa-manifest.json` and `public/.well-known/assetlinks.json`
 * (enforced by twa.test.ts). It can never change once the app is published.
 */
export const TWA_PACKAGE = "in.sirigannada.app";
